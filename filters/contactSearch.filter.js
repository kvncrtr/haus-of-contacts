(function () {
    'use strict';

    angular.module('apApp').filter('contactSearch', function () {
        function contains(nodes, target) {
            console.log(nodes || '').toLowercase();
            return 
        }
    })
})